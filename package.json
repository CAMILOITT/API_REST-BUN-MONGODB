{
  "name": "express",
  "module": "main.ts",
  "type": "module",
  "devDependencies": {
    "@types/bun": "latest",
    "@types/supertest": "^6.0.3"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "scripts": {
    "dev": "bun --hot run ./src/main.ts",
    "build": "bun build main.ts --outdir dist --target node18",
    "test": "bun test --watch --env-file=.env.test"
  },
  "dependencies": {
    "mongoose": "^8.17.0",
    "supertest": "^7.1.4",
    "zod": "^4.1.1"
  }
}
